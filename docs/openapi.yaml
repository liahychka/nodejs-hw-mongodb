openapi: 3.1.0
info:
  version: 1.0.0
  title: Contacts
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description: Phone book
tags:
  - name: Contacts
    description: Operation with contacts.
servers:
  - url: http://localhost:3000
  - url: https://hw7-swagger-v0ve.onrender.com/api-docs/
paths:
  /contacts/{id}:
    get:
      $ref: ../swagger/paths/contacts/{id}/get.yaml
    delete:
      $ref: ../swagger/paths/contacts/{id}/delete.yaml
    patch:
      $ref: ../swagger/paths/contacts/{id}/patch.yaml
  /contacts:
    post:
      $ref: ../swagger/paths/contacts/post.yaml
    get:
      $ref: ../swagger/paths/contacts/getContacts.yaml
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
